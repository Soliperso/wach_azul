# Flutter Standards

- Keep widgets small and readable.
- Keep business rules in controllers, repositories, or services.
- Use Material 3 and the shared app theme.
- Prefer responsive constraints over fixed full-screen assumptions.
- Verify with `dart format`, `flutter analyze`, and `flutter test`.
